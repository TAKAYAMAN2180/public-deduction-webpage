import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    // TODO:テーマ設定を行います
    palette:{
        primary:{
            main: '#d87274',
            light: '#ffa2a3',
            dark: '#a34449'
        }
    }
});

export default theme;