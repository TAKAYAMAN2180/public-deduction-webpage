import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    // TODO:テーマ設定を行います
    palette:{
        primary:{
            main: '#0067c0',
            light: '#54D4FF',
            dark: '#000099'
        }
    }
});

export default theme;